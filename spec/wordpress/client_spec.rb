require 'spec_helper'

describe Wordpress::Client do
  it 'has a version number' do
    expect(Wordpress::Client::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
